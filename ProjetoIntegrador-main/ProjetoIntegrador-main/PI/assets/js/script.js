// Arquivo JavaScript principal\n// Adicione aqui scripts para funcionalidades interativas, validações, etc.\n\ndocument.addEventListener(\'DOMContentLoaded\', () => {\n    console.log(\'script.js carregado.\');\n\n    // Exemplo básico de como ler um cookie (apenas para demonstração)\n    function getCookie(name) {\n        const value = \`; ${document.cookie}\`;\n        const parts = value.split(\`; ${name}=\`);\n        if (parts.length === 2) return parts.pop().split(\';\').shift();\n        return null;\n    }\n\n    // Exemplo: verificar se há um cookie de preferência\n    // const userPreference = getCookie(\'user_preference\');\n    // if (userPreference) {\n    //     console.log(\'Preferência do usuário encontrada:\', userPreference);\n    // }\n\n    // Adicionar listeners de evento, validações de formulário, etc. aqui\n}); 