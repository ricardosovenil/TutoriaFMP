name: flutter_application_1
description: Projeto Flutter

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Dependências existentes
  cupertino_icons: ^1.0.8
  sqflite: ^2.3.0
  path: ^1.8.3
  intl: ^0.19.0
  sqflite_common_ffi: ^2.4.0
  table_calendar: ^3.0.9
  file_picker: ^8.0.0+1
  path_provider: ^2.1.3
  open_file_plus: ^3.0.0

  # NOVA DEPENDÊNCIA PARA O EFEITO DE VIDRO
  glassmorphism: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
